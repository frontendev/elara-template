- var menu = {items: menu_items};
//
  script(type="text/javascript").
    var menu = !{JSON.stringify(structure)}
    console.log(menu);
    
nav.navbar.navbar-default
  div.container
    div.navbar-header
      button(type='button' class='navbar-toggle collapsed' data-toggle='collapse' data-target='#top-nav' aria-expanded='false')
        span.sr-only Menú
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand.hidden-sm(href='/es')
        img(src='/images/htl-9-julio-logo.png', alt='')
      div.container.visible-sm.mln15
        a.text-center.center-block(href='/')
          img.center-block.logo-sm(src='/images/htl-9-julio-logo.png', alt='Logo HTL')    
    div.collapse.navbar-collapse#top-nav
      ul.nav.navbar-nav.navbar-right
        //Start parsing the menu
        each menu in menu_items
          li
            a(href='#{menu[1]}' class='#{menu[3]}') #{menu[0]}  
